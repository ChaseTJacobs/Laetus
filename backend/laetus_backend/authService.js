var db = require("./dbService");
var accountService = require('./accountService');
// var cryptoUtil = require("./cryptoUtil");

exports.generateToken = function(user_email, user_pass, callback) {
	// DB access to get the user_ID
	
}
